import { ImageSourcePropType } from "react-native";

const mapImages: { [key: string]: ImageSourcePropType } = {
  fields: require("./fields.png"),
  mountains: require("./mountains.png"),
};

export default mapImages;
